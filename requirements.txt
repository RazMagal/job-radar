-r requirements-core.txt

# LinkedIn + Indeed sweeps. Not optional in practice: these are the only way to see
# postings from companies with no reachable ATS (Qualcomm, Google, AMD, Synopsys,
# Apple — see the bottom of config/companies.yaml).
#
# Pinned exactly, not floated. The package is effectively unmaintained (last publish
# 2025-07) and scrapes sites that change under it, so a new release is more likely to
# break the sweeps than to fix them. Bump this deliberately, after checking a scan
# still returns rows — never as a drive-by upgrade.
#
# Costs ~120 MB (pandas + numpy + pydantic + tls-client). If you want the lean
# install, use requirements-core.txt and the jobspy entries will fail loudly in
# `check` rather than silently returning nothing.
python-jobspy==1.1.82

# CV reading for `scan --deep` (Stage 2 CV matching). Local-only — the cloud scan
# never touches CVs, so these stay out of requirements-core.txt. Both are small and
# pure-Python. pypdf handles text PDFs (scanned/image PDFs need OCR, unsupported).
pypdf>=4.0
python-docx>=1.1

# ONLY for the Rafael board (type: rafael, ci: false). Its jobs API is behind a Reblaze
# WAF, so that one adapter drives a real browser to pass the challenge. Left COMMENTED —
# it pulls a browser driver and is useless without a Chrome/Chromium. Uncomment (and use
# your system Chrome via channel="chrome", or run `playwright install chromium`) only if
# you want Rafael; every other board works without it, and Rafael fails loudly if it's
# missing rather than breaking the run.
# playwright>=1.50
