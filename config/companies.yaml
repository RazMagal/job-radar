# Where to look. Each entry is one careers board.
#
# Every board below was verified live with `./jobradar.py check`. Re-run that after
# any edit — board tokens change, and a stale token otherwise fails silently as
# "zero jobs" rather than an error.
#
# How to find the token for a company:
#   greenhouse       boards.greenhouse.io/<board>        -> board: <board>
#   lever            jobs.lever.co/<board>               -> board: <board>
#   ashby            jobs.ashbyhq.com/<board>            -> board: <board>
#   smartrecruiters  jobs.smartrecruiters.com/<company>  -> board: <company>
#   bamboohr         <sub>.bamboohr.com/careers          -> board: <sub>
#   workday          <tenant>.<wd>.myworkdayjobs.com/<site>
#                    e.g. nvidia.wd5.myworkdayjobs.com/NVIDIAExternalCareerSite
#                    -> tenant: nvidia   wd: wd5   site: NVIDIAExternalCareerSite
#   comeet           comeet.com/jobs/<slug>/<uid>  -> uid: <uid>  token: <token>
#                    Comeet needs a per-company `uid` AND `token`. Both are baked into
#                    the Comeet-hosted page; harvest once, they're stable:
#                      curl -sL https://www.comeet.com/jobs/<slug>/<uid> \
#                        | grep -oiE '"(uid|token)": *"[^"]+"'
#                    The `token` is the company one near the top; the `uid` is the one
#                    in the URL. QUOTE both in YAML — "76.005" is otherwise read as a
#                    float. Comeet serves no job description, so its jobs match on title
#                    only (keep roles.yaml `match_any` rich: vlsi, dft, ...).
#
# Workday status codes (verified against a known-good board — they are the opposite
# way round from what you'd expect):
#   404 = tenant is right, `site` is wrong (read it off the careers page URL)
#   422 = no public Workday tenant by that name on that wdN — wrong tenant, wrong wd
#         number, or the company simply isn't on Workday
#   401 = internal-only board; skip it
#
# `enabled: false` parks an entry. `ci: false` skips it in GitHub Actions.
# `expect_min: N` fails the board when it returns fewer postings — set it on boards that
# are never legitimately thin (IAI, Elbit, Amazon...), where "0 jobs" otherwise looks
# identical to a silently rotted parser or a WAF block.

companies:
  # ---- Chip / silicon -------------------------------------------------------
  - name: NVIDIA
    type: workday
    tenant: nvidia
    wd: wd5
    site: NVIDIAExternalCareerSite
    search_text: Israel

  - name: Intel
    type: workday
    tenant: intel
    wd: wd1
    site: External
    search_text: Israel

  - name: Marvell
    type: workday
    tenant: marvell
    wd: wd1
    site: MarvellCareers
    search_text: Israel

  - name: Cadence
    type: workday
    tenant: cadence
    wd: wd1
    site: External_Careers
    search_text: Israel

  - name: Applied Materials
    type: workday
    tenant: amat            # not `appliedmaterials`
    wd: wd1
    site: External
    search_text: Israel

  # Broadcom and Analog Devices deliberately have no `search_text`: their CXS search
  # matches title/description text only, not location, so `search_text: Israel`
  # silently returns zero. Pulled unfiltered and narrowed by the location filter
  # in roles.yaml instead.
  - name: Broadcom
    type: workday
    tenant: broadcom
    wd: wd1
    site: External_Career

  - name: Analog Devices
    type: workday
    tenant: analogdevices
    wd: wd1
    site: External

  - name: Mobileye
    type: lever
    board: mobileye
    region: eu              # US endpoint 404s for this board

  - name: Tenstorrent
    type: greenhouse
    board: tenstorrent

  - name: SambaNova
    type: greenhouse
    board: sambanovasystems

  - name: Cerebras
    type: ashby
    board: cerebras

  - name: Etched
    type: ashby
    board: etched

  # ---- Israeli chip / deep-tech on Comeet -----------------------------------
  # Where most of the Israeli semiconductor scene actually hosts its jobs. uid+token
  # were harvested live from each Comeet-hosted page (see the header note); they're
  # per-company and stable. Comeet returns no description, so these match on title only.
  - name: Nuvoton               # chip architect, chip design, DV, emulation, SoC validation
    type: comeet
    uid: "51.00F"
    token: "15F57C6DB15FAF8C5757CAF841D6DB"

  - name: Ceva                  # DSP/IP — AI embedded, DSP, verification
    type: comeet
    uid: "76.005"
    token: "67526BE6752D332D33026BE135F033A8"

  - name: NextSilicon          # HPC accelerators — AI compiler, chip, verification
    type: comeet
    uid: "18.007"
    token: "817308A2873102E1845308A308A40B8308A205C"

  - name: proteanTecs          # chip telemetry — silicon, embedded, C++
    type: comeet
    uid: "D5.00E"
    token: "5DE23340029121D562912029122334"

  - name: NeuReality           # AI inference silicon — hardware, AI infra
    type: comeet
    uid: "89.002"
    token: "982428E9820428E390C55924C101C861C86"

  - name: Quantum Machines     # quantum control HW — FPGA, logic design, micro-architect
    type: comeet
    uid: "D6.000"
    token: "6D028E0DA022106D01470221036803D502210"

  - name: Innoviz              # LiDAR SoC — embedded, simulation, hardware
    type: comeet
    uid: "52.004"
    token: "25495012A0104C012A0104C6FCBA40"

  - name: DriveNets            # network cloud — power integrity, GPU SW, system architect
    type: comeet
    uid: "72.006"
    token: "2769D81626762C4E762762C4EC4E1626"

  - name: Xsight Labs          # fabless networking/AI silicon — VLSI, SRAM design, drivers
    type: comeet
    uid: "46.00C"
    token: "64C25C864C12E41930326064C25C801930"

  - name: Cellebrite           # digital forensics — embedded Linux, Python/Linux dev, SW
    type: comeet
    uid: "C3.00F"
    token: "3CF130BF3C0B6D16DA1AA9B6D130B16DA"

  - name: Arbe Robotics        # imaging-radar chips (Tel Aviv) — chip test, DSP, embedded SW, HW, algorithms
    type: comeet
    uid: "C6.001"
    token: "6C12886D821B0414432886144328862F472F47"

  - name: TriEye              # SWIR sensing chips (Tel Aviv) — analog chip design, EO/physics, NPI
    type: comeet
    uid: "A6.00E"
    token: "6AE2814D5CD5C1AB8140A35706AE2EC22814"

  - name: Wiliot              # battery-free IoT chips (Tel Aviv/Caesarea) — logic DV, firmware, RF integration
    type: comeet
    uid: "F6.003"
    token: "6F329B2DE6379829B229B2037983E8B3E8B"

  # More Tel Aviv startups host on a real ATS but PROXY it server-side, so there's no
  # client-exposed board API to harvest: Foretellix/Vtool/Lightbits (Comeet via WP), Weka
  # (Greenhouse, server-rendered). NeoLogic/AI21/Classiq render careers entirely in JS.
  # Rather than a fragile per-site scraper, they're covered by the company-targeted
  # Google-for-Jobs sweeps at the bottom of this file (Google has indexed their postings).
  # Upgrade any to a proper board later via a one-off browser capture if you want it.

  # Hailo's Comeet board is live but currently only a placeholder ("amazing openings!").
  # Flip enabled: true once it lists real reqs.
  - name: Hailo
    type: comeet
    uid: "D5.007"
    token: "5D71D33348F348F1D331D3311852EB85D7BAE"
    enabled: false

  # ---- AI labs --------------------------------------------------------------
  - name: Anthropic
    type: greenhouse
    board: anthropic

  - name: OpenAI
    type: ashby
    board: openai

  # ---- Israeli tech (Linux / backend / AI roles) ----------------------------
  - name: Via
    type: greenhouse
    board: via

  - name: Similarweb
    type: greenhouse
    board: similarweb

  - name: Riskified
    type: greenhouse
    board: riskified

  - name: Lightricks
    type: greenhouse
    board: lightricks

  # ---- Big tech via their own public career APIs (not a third-party ATS) -----
  # These run their own careers sites, but each exposes a public JSON search with a
  # server-side location filter — so we can pull just the Israel roles, same as an ATS.
  - name: Amazon               # Annapurna Labs silicon (Graviton/Trainium) + AWS + Stores
    type: amazon
    location: Israel
    country: ISR
    expect_min: 50             # ~145 IL reqs on a normal day

  # Qualcomm and Microsoft both run Eightfold's public "PCSX" search API (same shape,
  # different host+domain — see jobradar/sources/eightfold.py).
  - name: Qualcomm             # Snapdragon modem/RF/camera silicon — ASIC, firmware, validation
    type: qualcomm
    location: Israel

  - name: Microsoft            # Azure / security / AI R&D (Herzliya, Haifa, Tel Aviv)
    type: microsoft
    location: Israel

  # Google has no clean API left — this reads the careers-page batchexecute RPC. It's the
  # most fragile source (a Google deploy can rotate the RPC id, which `check` will catch);
  # worth it for ~100 IL roles incl. silicon validation and CPU design verification.
  - name: Google               # silicon validation, CPU DV, TPU, SoC power + SWE/AI
    type: google
    location: Israel

  # Texas Instruments on Oracle Recruiting Cloud. host/site/location_id were read off the
  # careers.ti.com hcmRestApi XHR; location_id is the Israel GeographyId facet.
  - name: Texas Instruments    # Ra'anana chip R&D — design verification, digital design, emulation
    type: oracle
    host: edbz.fa.us2.oraclecloud.com
    site: CX
    location_id: "300000000361478"
    job_url: "https://careers.ti.com/job/{id}"

  # ---- Israeli defense / aerospace primes ------------------------------------
  # Elbit runs its own Next.js careers site (elbitsystemscareer.com) that hydrates from a
  # single static JSON blob — /cron/jobs.json, ~680 open reqs WITH full descriptions, so
  # `scan --deep` catches the RF / DSP / FPGA / HW-verification roles whose title is
  # generic. Every posting on that portal is an Israel role (the JSON's `area` — North /
  # Center / Sharon / Shfela / South / Haifa / Jerusalem — is kept as location context).
  # No token, no auth; the detail page is /job/?jid={jobId}.
  - name: Elbit Systems        # radar/EW/C4I/avionics — RF, DSP, FPGA, HW verification (Netanya/Haifa/Holon/Ra'anana)
    type: elbit
    expect_min: 300            # ~650 open reqs on a normal day

  # IAI's careers site (jobs.iai.co.il) is WordPress behind a Reblaze WAF, but the WAF
  # only challenges the page nav — NOT the static jobs feed it loads. So a plain GET of
  # that asset returns all ~470 reqs (see jobradar/sources/iai.py). Content is HEBREW,
  # which is why roles.yaml carries Hebrew keywords (אימות/מכ"ם/עיבוד אותות/…) alongside
  # the Latin acronyms (RF/FPGA/DSP) that Israeli titles keep inline. Elta (IAI's radar/EW
  # subsidiary) posts into this same feed.
  - name: IAI                  # Israel Aerospace Industries (+ Elta) — RF, FPGA, DSP, אימות, מכ"ם
    type: iai
    expect_min: 200            # ~500 reqs on a normal day; a WAF block otherwise looks empty

  # Rafael's /api/jobs (~320 reqs) IS behind the Reblaze wall, so this one board drives a
  # real browser (Playwright + system Chrome) to pass the challenge — hence `ci: false` and
  # the optional playwright dep (see requirements.txt). Title-only, Hebrew. If Playwright
  # isn't installed it fails loudly for just this board; everything else keeps working.
  # Set `browser_channel: chromium` to use a Playwright-managed browser instead of Chrome.
  #
  # KNOWN BROKEN since ~2026-08-14: Reblaze tightened (same week it started blocking IAI
  # in CI) and now detects the automation protocol itself — the challenge stub loads its
  # detection lib and never proceeds, headless AND headed, even from a residential IP,
  # with AutomationControlled disabled. Google-for-Jobs indexes nothing from the walled
  # site either, so there's no sweep fallback. Left enabled so the failure stays loud;
  # check jobs manually at career.rafael.co.il until their WAF posture changes.
  - name: Rafael               # Rafael Advanced Defense Systems — FPGA/ASIC, RFIC, וריפיקציה, אלגוריתמים (Haifa)
    type: rafael
    ci: false
    expect_min: 150            # ~320 reqs on a normal day

  # ---- Space / satellite ------------------------------------------------------
  # Gilat (Petah Tikva) hosts on Comeet like the chip scene — uid/token harvested live
  # from comeet.com/jobs/gilat/39.005. The board mixes Israel HQ roles with its Peru /
  # Brazil / US (Wavestream) subsidiaries; the location filter keeps the Israel ones.
  - name: Gilat                # satellite comms — RF, modem, DSP, FPGA, embedded (Petah Tikva)
    type: comeet
    uid: "39.005"
    token: "935373E52DD52DD126A373E49A8935373E935"

  # Ramon.Space (space-resilient processors, Yokneam), hiSky (satellite IoT terminals),
  # NSLComm (satellite antennas) and ImageSat/ISI (EO imagery) all render careers from
  # WordPress server-side (admin-ajax) — no harvestable board API. Covered by the
  # company-targeted Google-for-Jobs sweeps at the bottom of this file, same as the
  # unharvestable Tel Aviv startups.

  # ---- Wired up but currently empty/thin (ready for when they post IL roles) --
  # Added so a future scan picks them up automatically. All verified live: the adapter
  # resolves; there just aren't (m)any Israel roles right now. `check` will show these
  # EMPTY (except Arm's ~2) until that changes — that's expected, not a failure.
  - name: Vayyar               # radar chips (Yehud) — Workable board, currently empty
    type: workable
    board: vayyar

  # Astroscale (on-orbit servicing; Israel R&D in Tel Aviv, ex-Effective Space) runs one
  # global BambooHR board. ~47 reqs today, all Tokyo/UK/France — the Israel location
  # filter keeps 0 until the Tel Aviv office posts again, at which point they surface
  # automatically. `check` will show the raw global count; that's expected.
  - name: Astroscale           # on-orbit servicing — GNC, avionics, embedded (Tel Aviv R&D)
    type: bamboohr
    board: astroscale

  # AMD has no country filter, so its adapter pages the whole board (~1000 jobs) and keeps
  # country_code == IL — heavy, and 0 IL today, so ci:false (run it locally on demand).
  - name: AMD                  # ex-Xilinx Israel R&D — 0 public IL reqs right now
    type: amd
    ci: false

  # Arm and Synopsys are HTML-only (no JSON feed) — narrow, isolated scrapers, so ci:false.
  # Arm has ~2 Ra'anana verification roles; Synopsys' Israel facet is empty right now.
  - name: Arm                  # careers.arm.com/search-jobs/Israel (HTML) — GPU/DV verification
    type: arm
    ci: false
    location: Israel

  - name: Synopsys             # synopsys.avature.net (HTML) — 21374 = Israel country facet
    type: avature
    ci: false
    host: synopsys.avature.net
    filter: "2001=21374&2001_format=3135&listFilterMode=1"

  # ---- Known-unreachable, documented so nobody re-tries them ----------------
  # Probed live and still out — no public JSON/HTML we can consume today:
  #
  #   Tower      jobs live in legacy SuccessFactors (career2.successfactors.eu, company
  #              towersemic) behind the AjaxService — session cookie + CSRF `ajaxSecKey`,
  #              no public JSON. The WordPress shell (careers.towersemi.com) just links out
  #              to it. Doable as a 2-step session scrape, but fragile; not worth it yet.
  #   Nokia      Avature — not yet probed (the `avature` type would likely fit).
  #   Apple, Western Digital   Workday exists but returns 401 (internal).
  #
  #   (IAI/Elta and Rafael WERE listed here as Reblaze-walled. Both are now wired up in the
  #   defense/aerospace section: IAI via its ungated static feed, Rafael via a browser-backed
  #   adapter — see above. Only the entries above remain genuinely out.)
  #
  # Israeli chip firms whose jobs live only on their own site (no third-party ATS):
  #   Valens (WordPress), Weebit Nano (own site), Silicom (WP job-openings), Speedata
  #   (Wix — has live VLSI/DV roles, apply by email), SatixFy (acquired by MDA Space).
  #   Pliops and Autotalks had Comeet boards now deactivated/empty (Autotalks -> Qualcomm).
  #   Run:ai is now NVIDIA — covered by the NVIDIA Workday entry above.
  #
  # These are otherwise best covered by the LinkedIn/Indeed sweeps below.

  # ---- LinkedIn + Indeed + Google-for-Jobs sweeps via jobspy ----------------
  # Installed by default via requirements.txt. Verified working for Israel on
  # 2026-07-20 from a residential IP: the boards returned real, on-topic Israeli
  # postings. LinkedIn and Google-for-Jobs block datacenter IPs, so these are
  # `ci: false` — run them locally. Glassdoor is unsupported (no Israel domain; it
  # aborts the batch).
  #
  # `google` here is Google *for Jobs* (an aggregator over many boards), not
  # Google-the-employer (that's the `google` company entry above). Its scraper wants
  # a natural-language query, so each sweep carries a `google_search_term`.
  #
  # One entry per search term: these are keyword searches, not full board dumps,
  # so each role you care about needs its own sweep.
  - name: LinkedIn/Indeed/Google — verification
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: verification engineer
    google_search_term: verification engineer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  - name: LinkedIn/Indeed/Google — chip design
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: ASIC RTL design engineer
    google_search_term: ASIC RTL chip design engineer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  - name: LinkedIn/Indeed/Google — linux
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: linux embedded developer
    google_search_term: linux embedded developer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  - name: LinkedIn/Indeed/Google — RF
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: RF engineer
    google_search_term: RF engineer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  - name: LinkedIn/Indeed/Google — DSP / signal processing
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: DSP signal processing engineer
    google_search_term: DSP signal processing engineer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  - name: LinkedIn/Indeed/Google — FPGA
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: FPGA engineer
    google_search_term: FPGA engineer jobs in Israel
    location: Israel
    country: israel
    results: 50
    hours_old: 336

  # ---- Company-targeted sweeps (startups with no harvestable board) ----------
  # These Tel Aviv startups host on Comeet/Greenhouse but PROXY it server-side (no
  # client-exposed uid/token) or render careers entirely in JS, so we can't hit a clean
  # board API. The fix that still reaches them: Google-for-Jobs has already INDEXED their
  # Comeet/Greenhouse postings, so a "<company> jobs in Israel" query pulls them in — with
  # structured location, so only the Israel roles pass the filter. Same `ci: false` +
  # residential-IP caveat as the role sweeps. `search_term` is the company name; the role
  # profiles then keep only the verification/chip/RF/DSP/linux/AI titles.
  - name: Weka (via search)          # AI-data-infra / storage — Linux, embedded, kernel
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Weka
    google_search_term: Weka jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: Foretellix (via search)    # ADAS/AV verification — verification, C++, simulation
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Foretellix
    google_search_term: Foretellix jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: Vtool (via search)         # chip debug/verification tools — verification, FPGA
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Vtool
    google_search_term: Vtool verification jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: Lightbits (via search)     # NVMe/TCP storage — Linux, kernel, embedded
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Lightbits
    google_search_term: Lightbits Labs jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: NeoLogic (via search)      # CPU/chip startup — chip design, verification, RTL
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: NeoLogic
    google_search_term: NeoLogic VLSI jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: AI21 Labs (via search)     # LLM research lab — ML/AI research, research engineer
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: AI21 Labs
    google_search_term: AI21 Labs jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: Classiq (via search)       # quantum computing software — algorithms, research
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Classiq
    google_search_term: Classiq quantum jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  # Caesarea Business Park chip scene (Wiliot and NeuReality above are also Caesarea):
  - name: ColorChip (via search)     # photonic ICs / optical sub-systems (Caesarea) — analog, ASIC, optics
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: ColorChip
    google_search_term: ColorChip jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  # Space startups with WordPress-proxied careers pages (see the space section above):
  - name: Ramon.Space (via search)   # space-resilient AI/DSP processors (Yokneam) — chip, DSP, embedded, FPGA
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: Ramon.Space
    google_search_term: Ramon Space jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: hiSky (via search)         # satellite IoT comms terminals — RF, modem, embedded
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: hiSky
    google_search_term: hiSky satellite jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: NSLComm (via search)       # expandable satellite antennas — RF, antennas, embedded
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: NSLComm
    google_search_term: NSLComm jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336

  - name: ImageSat (via search)      # EO satellite imagery (ISI, Or Yehuda) — algorithms, image processing
    type: jobspy
    ci: false
    sites: [indeed, linkedin, google]
    search_term: ImageSat
    google_search_term: ImageSat International jobs in Israel
    location: Israel
    country: israel
    results: 25
    hours_old: 336
